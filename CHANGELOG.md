# 更新日志

## v1.0.0 — 2026-09-19

第一个正式版。定位是**可以放心交给别人用**的角色包。

### 内容

- **角色** — 睡衣优香 Live2D 待机动画，APNG，15.87 秒无缝循环，1 倍速
- **音效** — 优香语音（按下）
- **点击序列** — 第 1 下余额卡 → 第 2 下 28 条随机台词 → 第 3 下 4 张随机表情包
- **提醒** — 余额预警（≤ ¥10）、今日预算（≥ ¥10）、每轮消耗，全部优香化
- **图库** — 5 张（逆转气泡模板 · 3 张头像 · 账单优香）

### 这次改了什么（相对早先的试用版）

**① 卸载现在是真正可逆的**

早先的卸载是「值还等于本包的 → 删掉」，结果是：

- 点击序列不还原，只留个 `.bak` 让你自己覆盖回去
- 用量设置回退到**出厂默认**，而不是你安装前的值
- 音效组一律回退到 `duck`，哪怕你原来用的是 `fx1`

现在安装时会写下 `.yuuka-pack-state.json`，记录**每一项被触碰之前是什么**；
卸载时按它精确还原。实测覆盖了：

- 你已有的角色 / 音效 / 气泡图 —— 原样保留
- 你安装前用的是 `fx1` —— 卸载后还是 `fx1`
- 你安装前有自己的预警文案 —— 卸载后原样回来
- **你的条目跟本包同 ID** —— 卸载后你的那条被完整还原（连 `pinnedAt` 都还在）

账本数据（`todayUsage` / `history` / `events` / `accounting`）全程不动，且写入后自动复核。

**② 所有 ID 加了命名空间**

```
旧：role_yuuka · audio_yuuka · group_yuuka · bimg_yuuka_*
新：joxin_yuuka_*（pajama / voice / click / bubble / face1~3 / bill）
```

早先的 ID 太通用 —— 别人如果已经有一个叫 `role_yuuka` 的角色，会被静默覆盖。
现在带前缀，冲突概率大幅降低；就算真撞上，也有上面的状态还原兜底。

**③ 护栏**

- 重复安装会拒绝，除非显式 `--force`（避免覆盖掉「最初安装前」的那份状态）
- 卸载时如果找不到状态文件，**拒绝执行** —— 宁可不卸，也不猜着删
  （真丢了可以用 `--force-best-effort`，那个模式只删本包已知的 id，不碰设置）

### 已知限制

- **提醒文案不支持随机语句。** 提醒的渲染函数只处理文本 / 图片 / 随机图片，
  拖一个「随机语句」模块进去会渲染成空行。所以余额预警和今日预算各只有一句，
  没法做「3~5 条随机」。这需要改上游代码。
- **角色选择存在浏览器的 localStorage**，安装脚本够不着 —— 装完要在
  「角色/资源管理」里手动选一次。

### 验证

安装→卸载的往返测试覆盖了：用户已有数据、同 ID 冲突、`soundSet` 非默认值、
账本完整性、重复安装护栏。六个配置文件的 JSON 内容在卸载后与安装前**逐字段一致**。
